name: Deploy Site

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  call-deploy-workflow:
    uses: mihailshterev/blueprints/.github/workflows/static-site-deploy.yml@main
